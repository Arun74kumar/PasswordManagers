/**
 * switch Toggle Style
 * @format
 */

import {ScaledSheet} from '@app/styles';

export const styles = ScaledSheet.create({
  container: {
    flex: 1,
  },
  innerCircleStyle:{
    alignItems: 'center',
    justifyContent: 'center',
    height: '12@ms',
    width: '12@ms',
    borderWidth:0
  }
});